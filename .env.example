# Base URL of the FastAPI chat backend.
# Defaults to http://localhost:8000 in code when unset (see src/lib/chatApi.ts).
# Copy this file to `.env` and adjust for other environments, e.g.:
#   VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=http://localhost:8000
